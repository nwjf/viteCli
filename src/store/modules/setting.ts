const state = {
    test: 'setting store test',
};
const getters = {};
const mutations = {};
const actions = {};
const store = {
    namespaced: true,
    state,
    getters,
    mutations,
    actions,
};

export default store;
